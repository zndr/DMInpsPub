<Window x:Class="DMInps.PatientSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ricerca Paziente" 
        Height="220" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
    </Window.Resources>
    
    <Border Padding="25" Background="White" Margin="10" CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Intestazione -->
            <TextBlock Grid.Row="0" 
                       Text="Inserire il codice fiscale del paziente:"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="#2C3E50"
                       Margin="0,0,0,12"/>

            <!-- Input Codice Fiscale -->
            <TextBox Grid.Row="1"
                     Name="txtCodiceFiscale"
                     FontSize="16"
                     CharacterCasing="Upper"
                     MaxLength="16"
                     Padding="8"
                     BorderBrush="#BDC3C7"
                     BorderThickness="2"
                     Margin="0,0,0,10">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="FontFamily" Value="Consolas"/>
                        <Style.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3498DB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <!-- Messaggio Errore -->
            <Border Grid.Row="2"
                    Name="borderErrore"
                    Background="#FFEBEE"
                    BorderBrush="#E74C3C"
                    BorderThickness="1"
                    CornerRadius="3"
                    Padding="10"
                    Margin="0,0,0,10"
                    Visibility="Collapsed">
                <TextBlock Name="txtErrore"
                           Text=""
                           Foreground="#C0392B"
                           TextWrapping="Wrap"
                           FontSize="12"/>
            </Border>

            <!-- Pulsanti -->
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Name="btnConferma"
                        Content="Conferma"
                        Width="110"
                        Height="35"
                        Margin="0,0,10,0"
                        Background="#27AE60"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Click="BtnConferma_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#229954"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <Button Name="btnAnnulla"
                        Content="Annulla"
                        Width="110"
                        Height="35"
                        Background="#95A5A6"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        IsCancel="True"
                        Click="BtnAnnulla_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#7F8C8D"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
